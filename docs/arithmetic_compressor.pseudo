byte[] compress(int16[] señales):
    int16 señal_anterior = 0
    byte[] salida
    for x in señales:
        int16 error = x - señal_anterior
        uint16 simbolo
        if error < 0:
            simbolo = 2 * error - 1
        else:
            simbolo = 2 * error
        primer_byte, segundo_byte = separar_bytes(simbolo)
        salida.append(codificador_aritmetico(primer_byte))
        salida.append(codificador_aritmetico(segundo_byte))
    return salida